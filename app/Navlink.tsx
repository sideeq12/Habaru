import React from 'react'

type Props = {
    category : string,
    isActive : boolean
}

function Navlink( {category, isActive } : Props) {
  return (
    <div>Navlink</div>
  )
}

export default Navlink